# Copy to secrets.toml (git-ignored) for local use, or paste the same block into
# the Streamlit Cloud app's Settings -> Secrets. Neon and Supabase both give you
# this URL; keep sslmode=require.
[connections.planner]
url = "postgresql+psycopg2://USER:PASSWORD@HOST/DATABASE?sslmode=require"
