# A mirror of the dependencies in pyproject.toml, for `pip install -r`.
# pyproject.toml is the source of truth; change it, re-run `uv sync` to update
# uv.lock, and keep this in step by hand.
streamlit>=1.64,<2
pandas>=2.0,<4
sqlalchemy>=2.0,<3
